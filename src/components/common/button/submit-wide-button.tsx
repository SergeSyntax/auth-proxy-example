import { styled } from '@material-ui/core/styles';
import { SubmitButton } from './submit/submit';

export const SubmitWideButton = styled(SubmitButton)``;

SubmitWideButton.defaultProps = { fullWidth: true };
