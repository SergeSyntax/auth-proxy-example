import { Avatar, styled } from '@material-ui/core';

export const IconButtonUserOptions = styled(Avatar)`
  color: #626262;
  background: #eef3f8;
  font-size: 1.6rem;
  transition: all 0.3s;
  &:hover {
    filter: brightness(0.9);
  }
`;
