import { styled } from '@material-ui/core';

export const WrapperRecoveryAlternativeLogin = styled('p')`
  font-size: 1.5rem;
  font-weight: bold;
  text-align: center;
  text-transform: capitalize;
`;
