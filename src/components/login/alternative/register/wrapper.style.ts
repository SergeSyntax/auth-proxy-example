import { styled } from '@material-ui/core';

export const WrapperRegisterAlternativeLogin = styled('p')`
  font-size: 1.5rem;
  margin: 1.5rem 0;
  text-align: center;
  text-transform: capitalize;
`;
