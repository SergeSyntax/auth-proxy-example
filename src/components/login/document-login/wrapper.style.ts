import { styled } from '@material-ui/core/styles';
import { Container } from '@material-ui/core';

export const WrapperDocumentLogin = styled(Container)`
  padding: 2rem 4rem;
  border-radius: 0.5rem;
`;

WrapperDocumentLogin.defaultProps = { maxWidth: 'xs' };
