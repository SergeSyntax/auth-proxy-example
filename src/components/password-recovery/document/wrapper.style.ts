import { styled } from '@material-ui/core/styles';

export const WrapperDocumentPasswordRecovery = styled('div')`
  && {
    margin-top: 0rem;
    max-width: 50rem;
    width: 100%;
    padding: 3rem 4rem;
    border-radius: 0.5rem;
    box-shadow: 0 10px 20px rgb(0 0 0 / 20%);
    background: #fff;
  }
`;
