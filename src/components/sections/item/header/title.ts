import { CardActionArea, styled } from '@material-ui/core';

export const TitleHeaderItemSection = styled(CardActionArea)`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  padding: 1rem;
  font-size: 1.5rem;
  font-weight: bold;
  height: 3.7rem;
`;
