import { DialogTitle, styled } from '@material-ui/core';

export const WrapperHeaderTask = styled(DialogTitle)`
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  :first-child {
    flex: 1;
  }
`;
