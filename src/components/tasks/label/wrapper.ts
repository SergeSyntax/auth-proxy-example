import { styled } from '@material-ui/core';

export const WrapperLabelTask = styled('label')`
  font-size: 1.7rem;
  display: inline-flex;
  align-items: flex-end;
  margin-bottom: 2rem;
  font-weight: bold;
  letter-spacing: 0.05rem;
`;
