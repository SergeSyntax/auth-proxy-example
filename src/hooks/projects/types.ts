export interface ProjectPayload {
  title: string;
  accessibility: boolean;
}

export interface ProjectData {
  id: string;
  title: string;
  accessibility: boolean;
  createdAt: string;
  updatedAt: string;
  owner: string;
}
